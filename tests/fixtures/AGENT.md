# Fixture Agent Rules

- Keep one canonical minimal valid fixture; derive targeted invalid copies/overlays.
- A manifest names expected file counts, fingerprint setup, and primary diagnostic per invalid.
- Never place real user source/save data, generated copyrighted prose, or secrets here.
- Validate valid fixture fully and assert each invalid fails for its named reason.
