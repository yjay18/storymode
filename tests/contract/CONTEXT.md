# Contract Test Context

Contract tests lock public Pydantic/JSON Schema, JSONL, LLM response, HTTP/OpenAPI, and
event shapes. Valid and intentionally invalid fixtures prove strict parsing and version
behavior independently of adapters.
