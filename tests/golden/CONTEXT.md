# Golden Test Context

This folder stores immutable schema/OpenAPI/prompt structural snapshots and old-version
migration inputs once versions exist. Goldens stabilize contracts, not volatile creative
wording, timestamps, generated IDs, local paths, or model-specific prose.
