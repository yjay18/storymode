# Prompt Context

Prompts are versioned adapters at an untrusted boundary. They improve proposal
quality but never enforce authoritative mechanics. Each role has one narrow purpose,
a strict output contract, bounded facts, stored few-shot cases, and deterministic
validation after parsing.

Runtime prompts work offline with configured local Ollama models. Research performed
during campaign design is recorded separately and is not fetched during play.
