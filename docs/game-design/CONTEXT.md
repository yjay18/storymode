# Game Design Context

This folder is the canonical source for player-visible rules. The engine implements
these rules exactly; prompts may explain or propose within them but never override
them. Campaign data can choose values only inside documented schema/balance bounds.

If a mechanic is not specified here, do not invent it while implementing an
unrelated slice. Record the question and add a dedicated design/ADR slice.
