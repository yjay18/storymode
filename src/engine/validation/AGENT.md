# Validation Engine Agent Rules

- Collect all safe diagnostics and sort by file, JSON pointer, then code.
- Resolve references by stable ID and expected type only; never display name.
- Keep graph traversal deterministic and formulas structured/integer-based.
- An invalid pack/state yields no partial usable aggregate.
- Add a focused invalid fixture whose asserted primary error is the new diagnostic.
