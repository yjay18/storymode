# Progression Engine Agent Rules

- No arbitrary respec, runtime-generated skill/recipe, or more than four equipped skills.
- Check all prerequisites before constructing one atomic new state and event set.
- Fusion never partially consumes sources/catalyst and enforces companion backup minimum.
- Test every missing prerequisite, combat-mode guard, replay, and input immutability.
