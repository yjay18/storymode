# Domain Rules Context

This folder owns small pure calculations and invariants shared by transitions: point
buy, stat modifiers, rational rounding, resource arithmetic, and primitive effect
rules. It accepts typed inputs and returns typed values/errors without I/O or state
repository knowledge.
