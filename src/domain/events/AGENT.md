# Domain Events Agent Rules

- Add an event/effect only with a documented producer, consumer, state invariant, and
  serialization contract.
- Prefer closed discriminated unions and stable lowercase codes.
- Never store callable/formula text, narrator prose, HTTP status, or absolute paths.
- Update runtime schemas, valid/invalid contract fixtures, and affected transition tests.
