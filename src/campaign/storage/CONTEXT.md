# Campaign Storage Context

This folder implements safe filesystem adapters for campaign packs, drafts, published
content, saves, logs, snapshots, and assets. It owns canonical JSON, path containment,
fingerprints, locking, fsync, atomic replacement, and crash inspection—not game rules.
