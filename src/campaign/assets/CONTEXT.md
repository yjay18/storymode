# Campaign Assets Context

This folder owns canonical art prompt assembly, deterministic cache keys, a bounded
optional local generation queue, safe cache installation, and deterministic fallback
descriptors. Assets are presentation-only and absence never invalidates gameplay.
