# Campaign Generation Context

This folder runs the fixed seven-stage design-time generation sequence through injected
local model ports, validates each typed draft artifact, repairs only its owning stage,
and persists progress. It never publishes or makes a draft playable.
