# LLM Retrieval Context

This folder builds immutable, display-safe, role-specific context packets from already
selected campaign/state data. It ranks optional summaries, preserves mandatory
dependencies, enforces byte budgets, and never retrieves the whole campaign/journal.
