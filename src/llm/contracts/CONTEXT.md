# LLM Contracts Context

This folder owns strict versioned Pydantic shapes returned by local models. Contracts
use candidate ordinals and semantic proposals; they never expose state patches, trusted
IDs, dice/DC decisions, executable formulas, paths, or authoritative outcomes.
