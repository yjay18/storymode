# LLM Orchestration Context

This folder coordinates context, prompt, local transport, strict parsing, bounded repair,
semantic validation, and deterministic fallback for each model role. It returns typed
proposals or display prose and has no state commit authority.
