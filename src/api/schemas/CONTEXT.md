# API Schemas Context

This folder owns versioned transport request, response, error, view, and allowed-command
DTOs. DTOs expose display-safe state and stable IDs without leaking domain internals,
hidden facts, raw prompts/responses, or local filesystem paths.
